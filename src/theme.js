import {createMuiTheme} from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
      primary: {
          main: '#40E0D0',
          contrastText: "white"
      },
      secondary: {
          main: '#00FFFF',
      },
      
  },
  });
  export default theme;